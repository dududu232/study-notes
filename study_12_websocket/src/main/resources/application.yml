server:
  port: 8888

engine:
  websocket:
    serverUrl:
    connectTimeout: 10    # 连接超时时间（单位：秒）  默认：10
    reconnectDelay: 5     #重连延迟间隔（单位：秒）   默认：5
    maxReconnectAttempts: 10    #最大重连尝试次数  当连接失败时，最多重试的次数  默认值：10次
    autoReconnect: true     #是否启用自动重连 true: 连接断开时自动重连  false: 不自动重连  默认值：true
